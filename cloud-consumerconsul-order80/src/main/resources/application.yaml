server:
  port: 80
spring:
  application:
    name: consumerconsul-order80
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
